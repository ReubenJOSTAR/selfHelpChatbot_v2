# ═══════════════════════════════════════════════════════════════
# WalkWithJesus — Render Production Dependencies
# Python 3.11 | Render | MongoDB Atlas | OpenAI Embeddings
#
# Dependency graph generation: LangChain 0.3.x + openai 1.x
#
# WHY 0.3.x and NOT 1.2.x:
#   langchain-openai 1.2.x requires openai>=2.26 (new openai 2.x SDK)
#   langchain-openai 0.3.x requires openai<2.0.0 (stable openai 1.x SDK)
#   The openai 2.x SDK is a breaking rewrite — not appropriate for a
#   stable production RAG backend. 0.3.x is the correct stable target.
#
# VERIFIED CONSTRAINT CHAIN:
#   langchain-openai==0.3.16  -> langchain-core<1.0.0,>=0.3.53
#                             -> openai<2.0.0,>=1.68.2
#   langchain==0.3.27         -> langchain-core<1.0.0,>=0.3.66
#   langchain-mongodb==0.10.0 -> langchain-core (compatible 0.3.x range)
#   openai==1.93.0            -> satisfies >=1.68.2,<2.0.0
#
# DO NOT pin langchain-core explicitly.
# DO NOT use langchain-openai>=1.0 with openai 1.x.
# DO NOT mix 0.3.x and 1.x langchain packages.
# ═══════════════════════════════════════════════════════════════

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Config & validation
pydantic==2.9.2
pydantic-settings==2.5.2

# LangChain 0.3.x ecosystem (openai 1.x compatible)
langchain==0.3.27
langchain-openai==0.3.16
# langchain-core is intentionally unpinned.
# langchain and langchain-openai declare compatible floors.
# Letting pip resolve prevents ResolutionImpossible errors.

# MongoDB vectorstore (official replacement for langchain-community)
langchain-mongodb==0.10.0

# OpenAI SDK
# Must be >=1.68.2 and <2.0.0 to satisfy langchain-openai 0.3.x.
# Pinned so Render cannot silently resolve a future 2.x release.
openai==1.93.0

# MongoDB driver
pymongo==4.8.0
dnspython==2.7.0

# Utilities
tiktoken==0.9.0
python-dotenv==1.0.1
certifi==2025.4.26
numpy==1.26.4
