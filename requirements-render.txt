# ─────────────────────────────────────────────
# WalkWithJesus — Backend Dependencies
# Validated compatible stack for Render / Python 3.11
# Last verified: May 2026
# ─────────────────────────────────────────────

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Config & validation
pydantic==2.9.2
pydantic-settings==2.5.2

# OpenAI — must be >=1.40 to match langchain-openai>=0.2
# 1.30.x removed `proxies`; langchain-openai>=0.2 targets the new interface cleanly
openai==1.51.0

# LangChain — upgraded to current stable (0.3.x series)
# langchain-openai>=0.2 no longer passes `proxies` to the OpenAI client
langchain==0.3.7
langchain-core==0.3.15
langchain-openai==0.2.9

# MongoDB — use langchain-mongodb instead of langchain-community for vectorstore
# langchain-community MongoDBAtlasVectorSearch is deprecated upstream
# langchain-mongodb is the official maintained replacement
langchain-mongodb==0.3.0
pymongo==4.8.0          # 4.8 is stable; avoids the TLS regression in 4.17
dnspython==2.7.0

# Utilities
python-dotenv==1.0.1
certifi==2024.8.30
numpy==1.26.4
tiktoken==0.7.0
